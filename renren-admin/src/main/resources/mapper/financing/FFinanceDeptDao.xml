<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.financing.dao.FFinanceDeptDao">

	<select id="getList" resultType="io.renren.modules.financing.entity.FFinanceDeptEntity">
		select t1.*,(select t2.name from f_finance_dept t2 where t2.id=t1.pid)parentName from f_finance_dept t1
		<where>
			<if test="deptIdList != null">
				t1.id in
				<foreach item="id" collection="deptIdList" open="(" separator="," close=")">
					#{id}
				</foreach>
			</if>
		</where>
		order by t1.sort asc
	</select>

	<select id="getById" resultType="io.renren.modules.financing.entity.FFinanceDeptEntity">
		select t1.*,(select t2.name from f_finance_dept t2 where t2.id=t1.pid)parentName from f_finance_dept t1
		where t1.id = #{value}
	</select>

	<select id="getIdAndPidList" resultType="io.renren.modules.financing.entity.FFinanceDeptEntity">
		select t1.id, t1.pid from f_finance_dept t1
	</select>

	<select id="getSubDeptIdList" resultType="long">
		select id from f_finance_dept where pids like #{id}
	</select>

	<select id="getDept" resultType="io.renren.modules.financing.dto.FIndirectDeptDTO">
		select a.name,
		       a.type,
		       b.name as dept_name
		from f_finance_dept a
		JOIN sys_dept b
		on a.id = #{institution}
		and b.id = #{deptId}
	</select>

	<select id="getDeptName" resultType="String">
		select name from sys_dept where id = #{deptId}
	</select>

	<select id="getInstitutionName" resultType="String">
		select name from f_finance_dept where id = #{deptId}
	</select>

	<select id="getAllName" resultType="String">
		select name from sys_dept
	</select>

	<select id="getIdByName" resultType="String">
		select id from sys_dept where name = #{name}
	</select>

	<select id="getApprovalAmount" resultType="java.math.BigDecimal">
		SELECT issue_quota
		FROM f_direct_financing_approval
		WHERE approve_no = #{approveNo}
	</select>

</mapper>